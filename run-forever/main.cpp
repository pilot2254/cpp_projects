#define WIN32_LEAN_AND_MEAN // i hate windows bloat :3
#include <Windows.h>
#include <iostream>

int main() {
        std::cout << "alr buddy i have no idea why but here you go..." << '\n';
        while (true) {Sleep(INFINITE); }
        return 0;
}

// use -static flag
